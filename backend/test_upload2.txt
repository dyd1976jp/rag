第一章：父子关系测试

这是一个用于测试父子文档关系的新文档。
每个段落都应该有唯一的ID，不会相互覆盖。

第二章：向量存储测试

向量存储中的每个段落都应该：
- 有唯一的主键ID
- 包含正确的dataset_id
- 保持父子关系的完整性

第三章：搜索功能测试

用户搜索时应该能够：
1. 找到父文档和子文档
2. 正确应用用户权限过滤
3. 返回完整的元数据信息

第四章：总结

这个测试文档用于验证修复后的父子关系功能。
